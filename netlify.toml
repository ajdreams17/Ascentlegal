[build]
  command = "npm run build"
  publish = ".next"

[functions]
  node_bundler = "esbuild"

[[plugins]]
  # Pin the older runtime that supports Forms
  package = "@netlify/plugin-nextjs@4"

# (optional) keep lighthouse
[[plugins]]
  package = "@netlify/plugin-lighthouse"
